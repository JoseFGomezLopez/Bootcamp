const object = {
    a : 'jose',
    b : 'pedro'
}
//Devuelve un array de las claves en string
//combinado con el for in

console.log(Object.keys(object));
// for (const key in Object.keys(object)) {
//     console.log(key);
//     }

